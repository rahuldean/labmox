# labmox
Home lab services and scripts

## Folders
* `homelab-infra` - OpenTofu config for spinning up a Proxmox homelab. Deploys LXC containers on a single node — a reverse proxy, DNS server, Docker host, and NAS.